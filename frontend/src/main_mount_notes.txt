AIR-2.1I main.js integration point:

Import:
import { mountFinalEvidencePanel } from './finalMapEvidenceMount.js';

Call after map application initialization:
mountFinalEvidencePanel();

Result:
Map + Evidence Panel in one UI.
